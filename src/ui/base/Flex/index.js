import { Flex as FlexBase } from '@rebass/emotion'
import styled from '@emotion/styled'
import { flexBasis } from 'styled-system'

const Flex = styled(FlexBase)`
  ${flexBasis};
`

export default Flex